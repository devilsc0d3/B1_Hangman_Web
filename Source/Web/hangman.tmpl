<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>HANGMAN</title>
    </head>

    <style>
        body {background: #7922ad; padding: 0; margin: 0}
        h1   {color: blue;font-size:105px; text-align:center}
        p    {color: red;font-size:100px; text-align:center}

        form {color: rgba(0, 255, 104, 0.58);font-size:50px; text-align:center}
        input {color: rgba(0, 0, 0, 0.58);font-size:50px; text-align:center}
        button {color: rgba(0, 0, 0, 0.58);font-size:50px; text-align:center}
    </style>

    <body>
        <h1>{{.Title}}</h1>
        <p> {{.MotBase}}</p>
        <p> {{.Mot}}</p>

        <form>
            <input type="text" id="letter" name="wordletter"
                   placeholder="Entrez une lettre ou un mot" required
                   minlength="1" maxlength={{.LengthWord}}>
            <br>
            <br>
            <button value="submit"> Envoyer </button>
        </form>

    </body>

</html>